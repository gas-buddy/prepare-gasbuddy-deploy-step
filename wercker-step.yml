name: prepare-gasbuddy-deploy
version: 2.4.0
description: Prepare a wercker build for efficient deployment to a container registry
keywords:
  - deployment
  - container
properties:
  rebuild:
    type: string
    required: false
    default: "@gasbuddy/service"
