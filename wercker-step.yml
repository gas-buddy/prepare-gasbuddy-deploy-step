name: prepare-gasbuddy-deploy
version: 2.8.0
summary: Prepare a wercker build for efficient deployment to a container registry
tags:
  - deployment
  - container
properties:
  rebuild:
    type: string
    required: false
    default: "@gasbuddy/service"
